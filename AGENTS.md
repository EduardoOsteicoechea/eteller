# eteller

Read [`.eteller/AGENTS.md`](.eteller/AGENTS.md) first (hard stop after base; **ask before each wave** — do not start wave work until the user asks).

Product workspaces (`base/`, `waves/`) are generated at this root. Framework wiring lives under `.eteller/`.
