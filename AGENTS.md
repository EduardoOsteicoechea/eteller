# eteller

Read [`.eteller/AGENTS.md`](.eteller/AGENTS.md) first (includes **hard stop after base** — ask for the user story before inventing waves/tasks).

Product workspaces (`base/`, `waves/`) are generated at this root. Framework wiring lives under `.eteller/`.
